# V1 Engineering Review — 2026-08-22

## Scope
Review the current V1 architecture without changing the commercial directives: one WhatsApp number during V1; future numbers only after V1 is fully validated.

## Confirmed controls
- SDR/state machine remains authoritative over AI wording.
- Idempotency and state-transition guards are present.
- Single-number pilot gate is present in the inbound pipeline.
- AI secrets are server-side only.
- Meta webhook adapter reads secrets from server environment.
- Attribution is normalized before CRM use.
- Privacy/data minimization policy exists.
- Pilot readiness and kill-switch controls exist.
- Real pilot matrix and report template exist.
- Landing page has an attribution bridge prepared; activation must preserve the existing site content.

## Operational blockers before public V1
1. Deploy a public HTTPS backend endpoint.
2. Configure real Supabase/Meta/OpenAI environment variables securely.
3. Configure Meta webhook verification and callback for the V1 phone number.
4. Execute the real pilot test matrix.
5. Verify audio/transcription in the real environment.
6. Verify failure/retry and rollback behavior.
7. Activate the landing-page bridge only after the public backend endpoint is ready.

## Security note
No secret should be placed in the landing page or committed to Git. The previously supplied OpenAI credential should be treated as compromised and rotated/revoked before any real deployment.

## Release gate
V1 is not public-ready until the operational blockers above are validated. Future WhatsApp numbers remain disabled until V1 passes the release gate.
